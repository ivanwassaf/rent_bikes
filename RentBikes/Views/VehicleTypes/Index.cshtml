@model IEnumerable<RentBikes.Core.Domain.VehicleType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.State.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.State.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.vehicleTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.vehicleTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.vehicleTypeID })
        </td>
    </tr>
}

</table>
